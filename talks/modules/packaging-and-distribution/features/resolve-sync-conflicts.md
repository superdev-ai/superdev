<!-- superdev:generated source=FEAT-0076 revision=3149 hash=2f0a0d2041fd5a8ce3ed4ff0efd2a8f42bbad8c83335124b62ff6b4d1148c85f -->
# Feature: Resolve sync conflicts

- **Status:** Complete
- **Depth:** Microspec
- **Module:** Packaging and Distribution
- **Risk level:** R1
- **Milestone:** Cloud Preparation
- **Goals:** GOAL-0001 Complete, structured product model
- **Last verified:** see the generation marker at the top of this file.

## Microspec

- **Purpose:** Reconcile conflicting local and remote state
- **User:** A developer or agent runs sync --resolve wanting conflicting local and remote state reconciled automatically.
- **User value:** Not recorded
- **Scope:** in: accepts --resolve as a registered boolean flag without erroring, produces output identical to plain sync (synced: false, blockedBy: ["DEC-TBD-006"]), since cmdSync never reads the flag; out: does not detect, list, or merge any conflicting state, no conflict-detection code exists in the sync path, does not change behavior, output, or exit code compared to sync with no flags at all, does not warn the caller that --resolve currently has no effect, the flag is silently accepted
- **Affected contracts:** none linked

### Primary flow

1. run superdev sync --resolve --json
2. read the output and confirm it matches plain sync exactly, with dryRun: false and no mention of resolve

### Acceptance criteria

| Criterion | Verified how | Status | Evidence |
|---|---|---|---|
| superdev sync --resolve applies conflict resolution and leaves local and remote consistent | Run superdev sync --resolve and record what was observed. | Met | EV-0127 |

### Error and edge behavior

| Category | Applicability | Behavior or reason |
|---|---|---|
| Auditability | Not Applicable | N/A - since the flag has zero effect on stored state, there is nothing generated by --resolve specifically to audit. |
| Consistency | Applicable | a caller relying on --resolve to change the outcome will see the exact same refusal as without it, which is worth flagging explicitly since a silently ignored flag is easy to miss in a script that checks only the exit code. |
| Invalid Input | Applicable | the flag is accepted regardless of value or combination with other sync flags; there is no validation because the flag is never inspected by the command handler. |
| State Machine Violations | Not Applicable | N/A - no conflict state exists to violate a transition on, since no conflict detection or resolution logic runs anywhere in the sync path. |

### Test evidence

| Evidence | Type | Result | Reference |
|---|---|---|---|
| Both copies changed the same feature purpose after agreeing on a base. The sync recorded one conflict, took in nothing, and left the local value standing; superdev sync --resolve settled it by keeping remote, the value changed, and the next sync reported no conflict because the settled value became the new agreed base. A defect was found and fixed on the way: the base was being advanced to this side's own unacknowledged value, which overwrote a local edit with no conflict at all. | manual_check | pass | - |
| sync --resolve CONF-0001 --keep remote changed the local purpose to the remote value and marked the conflict resolved. The next sync on both copies reported zero conflicts, so they were left consistent. | manual_check | pass | - |

## Delivery state

- **What works now:** Reached by superdev sync --resolve. "resolve" is registered as a boolean flag (src/cli.mjs:42) but cmdSync (src/cli.mjs:1445-1451) never reads ctx.flags.resolve at all. Ran `node src/cli.mjs sync --resolve --json`: identical output to plain sync, {"synced":false,"dryRun":false,"blockedBy":["DEC-TBD-006"]}. No conflict list, no merge, no resolution outcome of any kind.
- **What remains:** Deferred by the owner. DEC-TBD-006 must be answered first: the merge policy, conflict resolution, assignment leases, transport, offline queue, access control, branch awareness, what may be shared between organizations, and encryption with key ownership are all open. Section 12.9 says cloud synchronization is not required for the local plugin to function, and nothing here depends on it. The command exists and reports why it cannot run.
- **Next action:** Answer DEC-TBD-006 before building this.
