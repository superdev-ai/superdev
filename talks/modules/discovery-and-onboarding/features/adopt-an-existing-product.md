<!-- superdev:generated source=FEAT-0002 revision=2943 hash=1c91eaf507462c4863ef10fe9ea631bcc024707d3e7031b9dab924edca708c7d -->
# Feature: Adopt an existing product

- **Status:** Complete
- **Depth:** Microspec
- **Module:** Discovery and Onboarding
- **Risk level:** R1
- **Milestone:** Initialization and Adoption
- **Goals:** GOAL-0001 Complete, structured product model
- **Last verified:** see the generation marker at the top of this file.

## Microspec

- **Purpose:** Reverse-engineer an existing codebase into a product map without changing behavior
- **User:** An engineering lead onboarding an existing codebase wants Superdev to map what is already there without altering or duplicating a single line of existing code or docs.
- **User value:** Not recorded
- **Scope:** in: Detects existing dependencies (for example an express dependency in package.json) and answers material questions from that confirmed evidence, skipping questions the repo already answers, Plans and lists the exact actions it will take: create the control database, write talks/project.yaml, append a .gitignore entry, seed capability areas, stack slots and task categories, States explicit refusals: existing documentation is never moved, renamed, rewritten or copied into the database, and no Markdown is generated by adoption, Applying writes talks/project.yaml, appends the .gitignore entry and creates the control database, leaving existing files such as package.json and src untouched; out: Does not generate any Markdown documentation, unlike init, Does not migrate or rewrite existing documentation into a new profile or layout, Does not run init's foundation-document creation step at all
- **Affected contracts:** none linked

### Primary flow

1. Run superdev adopt --root <dir> as a dry run in a repo with existing code
2. Review the detected kind, confirmed evidence such as the express dependency, and the listed actions and refusals
3. Run superdev adopt --root <dir> --apply to write the adapter and control layer
4. Confirm existing files such as package.json and src/index.js are unchanged

### Acceptance criteria

| Criterion | Verified how | Status | Evidence |
|---|---|---|---|
| Running superdev adopt on an existing codebase produces an initial product model matching current behavior | Run superdev adopt and record what was observed. | Met | EV-0091 |

### Error and edge behavior

| Category | Applicability | Behavior or reason |
|---|---|---|
| Duplication | Applicable | Re-running adopt on an already-adopted repo reports each action as already present or leave exactly as it is, instead of rewriting talks/project.yaml or the .gitignore entry a second time. |
| Empty States | Applicable | When no .gitignore file exists yet, adoption creates one containing only the .superdev/ ignore line rather than failing. |
| State Machine Violations | Applicable | When the documentation profile cannot be detected, adoptProject warns that it will record the profile as custom and assume nothing about the layout, rather than guessing a profile. |

### Test evidence

| Evidence | Type | Result | Reference |
|---|---|---|---|
| adopt runs and reports its plan without writing anything until asked. It printed: Adoption plan ------------- Root this directory At 2026-07-27 20:28:31 Apply no Name Superdev Detection: Root this directory Kind Initialized Initialized yes Route reinit Docs Profile cus | command | pass | node src/cli.mjs adopt |

## Delivery state

- **What works now:** Reached by superdev adopt (plan), superdev adopt --apply (create). src/cli.mjs:336 cmdAdopt calls adoptProject from src/init/index.mjs. Ran adopt in a scratch dir containing a package.json with an express dependency and a src/index.js: it correctly reported Kind 'Existing Code', inferred 'Backend boundaries' and 'API style' from the express dependency (epistemic: confirmed, evidence: package.json) so those questions were never asked, and listed the exact actions it would take (create .superdev/, talks/project.yaml, .gitignore entry, seed capability areas/stack slots/task categories) plus explicit refusals ('Existing documentation is not moved, renamed, rewritten or copied into the database', 'No Markdown is generated by adoption'). Running with --apply wrote talks/project.yaml, .gitignore, and .superdev/superdev.db, leaving the existing package.json and src/index.js untouched.
- **What remains:** Nothing known.
- **Next action:** Not recorded
